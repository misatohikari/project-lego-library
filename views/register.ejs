<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lego Collection</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>

<body tabindex="-1">

  <%- include('partials/navbar', {page: '/register' }) %>

    <div class="container mx-auto p-4">
      <div class="hero bg-base-200 rounded-lg p-6">
        <div class="hero-content text-center ">
          <div class="max-w-md">
            <h1 class="text-5xl font-bold">Register</h1>

            <p class="py-6">Please provide your credentials below</p>

          </div>
        </div>
      </div>

      <br />

      <% if (errorMessage) { %>
        <div class="alert alert-error">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
          <span><%= errorMessage %></span>
        </div> 
      <% } %>

      <% if (!successMessage) { %>
      <form action="/register" method="post" class="space-y-4">
        
       
        <div class="flex flex-col">
            <label for="userName" class="text-sm font-semibold text-gray-600 bold-text">User Name</label><br/>
            <input type="text" id="userName" name="userName" placeholder="User Name" required
                value = "<%=userName %>"
                class="mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:border-blue-500"
                style="width: 100%;"
                autofocus
                />
        <div>

        <div class="flex flex-col">
            <label for="password" class="text-sm font-semibold text-gray-600 bold-text">Password</label><br/>
            <input type="password" id="password" name="password" placeholder="Password" required
            class="mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:border-blue-500"
            style="width: 100%;"
            />
        </div>

        <div class="flex flex-col">
            <label for="password2" class="text-sm font-semibold text-gray-600 bold-text">Confirm Password</label><br/>
            <input type="password" id="password2" name="password2" placeholder="Confirm Password" required
            class="mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:border-blue-500"
            style="width: 100%;"
            />
        </div>

        <div class="flex flex-col">
            <label for="email" class="text-sm font-semibold text-gray-600 bold-text">Email Address</label><br/>
            <input type="email" id="email" name="email" placeholder="Email Address" required
            <% if (email) { %>value="<%= email %>"<% } %>
            class="mt-1 p-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:border-blue-500"
            style="width: 100%;"
            />
        </div>
<br/>
        <input
            type="submit"
            class="custom-submit-button"
            value="Register"
        />
        </form>
        <% }else{ %>
            <div class="successAlert">
                <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                <span><%= successMessage %></span>
              </div><br />
              <a href="/login" class="btn btn-success">
                Proceed to Log in
              </a>              
        <% } %>

        
    </div>
</body>
</html>